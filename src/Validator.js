// export default class Validator {
//   constructor() {
//     console.log('constructor');
//   }
// }
import _ from 'lodash';

export default (num) => {
  const result = num / 2;
  _.noop();
  return result;
};
